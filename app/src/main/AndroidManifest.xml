<manifest xmlns:android="http://schemas.android.com/apk/res/android">
    <uses-permission android:name="android.permission.INTERNET"/>
    <application
        android:label="ESPNApp"
        android:theme="@style/Theme.Material3.DayNight.NoActionBar">
        <activity android:name=".ui.player.PlayerDetailActivity"/>
        <activity android:name=".ui.teamdetail.TeamDetailActivity"/>
        <activity android:name=".ui.teams.TeamsActivity"/>
        <activity android:name=".ui.matches.MatchesActivity"/>
        <activity android:name=".ui.news.NewsActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>
</manifest>
